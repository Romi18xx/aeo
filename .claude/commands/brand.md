---
description: Read or update the durable brand knowledge base (brand-memory skill)
---

$ARGUMENTS

If the text above is empty, load `brand-memory` and summarize the current
brand voice, positioning, audience, and known competitors. If it states a
new or corrected fact, update `.claude/brand-memory.json` through the
`brand-memory` skill instead of just answering in chat.
