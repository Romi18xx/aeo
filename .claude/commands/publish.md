---
description: Repurpose and publish/schedule the latest draft per channel (channel-publisher skill)
---

Take the latest content-writer draft and prepare it for these channels: $ARGUMENTS

If no channels were given above, ask which channels to target (e.g. blog,
LinkedIn, X, email) rather than guessing. Publish or schedule through a
connected integration when one exists — with explicit confirmation before
anything goes out — otherwise produce a ready-to-post package and schedule.
