---
description: Run a live AEO signal audit for a brand/domain (aeo-signal skill)
---

Run an AEO signal audit for $ARGUMENTS.

If no domain/brand was given above, use the `brand-memory` skill first to
find this project's own brand and domain, and audit that instead of asking
the user to repeat information already on file.
